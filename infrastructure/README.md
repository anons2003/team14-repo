# Infrastructure

Place Team 14 infrastructure-as-code here.

Recommended scope for W7:

- S3 frontend bucket
- CloudFront distribution
- S3 upload bucket
- API Gateway HTTP API
- Lambda function and execution role
- DynamoDB table
- CloudWatch dashboard, custom metric alarm, and log retention
- Optional VPC endpoints if the Lambda runs inside a VPC

